default_platform(:ios)

platform :ios do
  desc "Build archive for App Store / TestFlight"
  lane :archive do
    # Requires Apple Developer credentials in CI secrets.
    # gym(scheme: "Piercast", export_method: "app-store")
    UI.message("Manual archive: open apps/ios in Xcode → Product → Archive")
  end

  desc "Upload to TestFlight when credentials present"
  lane :beta do
    archive
    # upload_to_testflight
  end
end
